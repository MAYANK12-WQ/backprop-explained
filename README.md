# backprop-explained
An **interactive, Colab-ready** notebook that builds a tiny autograd engine (like micrograd)  from scratch, spells out **backpropagation step by step**, and makes gradients visible.  
